@using Bookish.Models;
@model BookViewModel;

@using (Html.BeginForm("SearchBook","Books",FormMethod.Post)){
    @* <label asp-for="@Model.Search.SearchInput">Search: </label>
    <input asp-for="@Model.Search.SearchInput"></input> *@
    <div>
     @Html.TextBoxFor(m => m.Search.SearchInput)
    </div>
    <button type="submit">Submit</button>
}

@* add a if condition, if searchresultlist is null,don't show *@
@* or, show all books by default *@
<div>
<table>
    <tr>
    <th>Title</th>
    <th>Author</th>
    <th>Id</th>
    <th>Check-out Date</th>
    <th>Check-in Date</th>
    <th>Member ID</th>
</tr> 

@foreach(BookModel book in Model.SearchResultList){
<tr>
    <td>@book.Title</td>
    <td>@book.Author</td>
    <td>@book.Id</td>
    <td>@book.CheckOut</td>
    <td>@book.CheckIn</td>
    <td>@book.MemberId</td>
</tr> 
}

</table>
</div>




